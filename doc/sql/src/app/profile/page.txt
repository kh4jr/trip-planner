#### [SECTION 1] Project Repository
- Provide the GitHub link for this project: https://github.com/kh4jr/trip-planner.git

#### [SECTION 2] Database Architectural Review & CRUD Mapping
Plik `page.tsx` (w katalogu `profile`) to komponent serwerowy Next.js renderujący profil osobisty zalogowanego użytkownika (dashboard podróżnika). Odpowiada za pobranie z bazy danych wszystkich podróży, których zalogowany użytkownik jest właścicielem (`ownerId`), oraz obliczenie na tej podstawie globalnych statystyk podróżniczych i wydatków.

Identyfikacja operacji CRUD:
- **Select (Odczyt)**:
  - `db.trip.findMany` – Pobranie wszystkich podróży powiązanych z zalogowanym użytkownikiem (`ownerId = userIdNumber`) wraz z ich powiązanymi wydatkami (`expenses`). Wyniki są sortowane malejąco według daty rozpoczęcia (`startDate: 'desc'`), co pozwala pokazać najnowsze wyjazdy jako pierwsze w archiwum.

Interpretacja SQL:
Metoda `db.trip.findMany` z filtrem właściciela oraz złączeniem wydatków tłumaczy się na zestaw zapytań SQL:
```sql
-- Pobranie wycieczek użytkownika o zadanym ownerId
SELECT "id", "name", "destination", "startDate", "endDate", "ownerId", "description", "location", "createdAt" 
FROM "Trip" 
WHERE "ownerId" = $1 
ORDER BY "startDate" DESC;

-- Pobranie powiązanych wydatków dla wycieczek (Eager Loading zoptymalizowany przez Prisma ORM)
SELECT * 
FROM "Expense" 
WHERE "tripId" IN ($1, $2, ...);
```
Wyszukiwanie filtru `WHERE "ownerId" = $1` jest wydajne, ponieważ kolumna `ownerId` reprezentuje klucz obcy (Foreign Key) wskazujący na tabelę `"User"`, co zazwyczaj wiąże się z założeniem indeksu w relacyjnej bazie danych, zapobiegając pełnemu skanowaniu tabeli (Full Table Scan).

#### [SECTION 3] Code Review & Explanatory Comments
```typescript
import React from 'react';
import Link from 'next/link';
import { getServerSession } from "next-auth";
import { authOptions } from "@/app/api/auth/[...nextauth]/route"; 
import { db } from "@/lib/db";
import { redirect } from "next/navigation";
import WorldMap from "@/components/WorldMap";

interface StatCardProps {
  title: string;
  value: string | number;
  sub: string;
  icon: string;
}

interface ArchiveItemProps {
  title: string;
  date: string;
  cost: string;
  status: string;
  isBlue?: boolean;
}

export default async function ProfilePage() {
  // Pobranie danych sesji zalogowanego użytkownika za pomocą NextAuth.
  const session = await getServerSession(authOptions);

  // Zabezpieczenie przed dostępem nieautoryzowanym: przekierowanie niezalogowanych użytkowników.
  if (!session?.user?.id) {
    redirect("/");
  }

  // Rzutowanie identyfikatora użytkownika na typ numeryczny w celu dopasowania do schematu bazodanowego (typ Int).
  const userIdNumber = parseInt(session.user.id);

  // Pobranie z bazy danych wszystkich podróży zalogowanego użytkownika.
  // Zastosowanie include: { expenses: true } pozwala na pobranie wydatków za jednym razem,
  // co pozwala uniknąć problemu zapytań N+1 podczas wyliczania łącznych kosztów każdej z podróży.
  const userTrips = await db.trip.findMany({
    where: { ownerId: userIdNumber },
    include: { expenses: true },
    orderBy: { startDate: 'desc' } // Sortowanie malejąco w celu ułożenia najnowszych wypraw na początku listy.
  });

  // Agregacja statystyk podróżniczych w pamięci serwera.
  const totalTrips = userTrips.length;

  // Obliczenie sumy wszystkich wydatków we wszystkich wycieczkach.
  const totalExpensesSum = userTrips.reduce((acc, trip) => {
    const tripSum = trip.expenses?.reduce((s, e) => {
      const val = e.amount ? Number(e.amount.toString()) : 0;
      return s + (isNaN(val) ? 0 : val);
    }, 0) || 0;
    return acc + tripSum;
  }, 0);

  // Filtracja wycieczek ukończonych (których data końcowa minęła).
  const completedTrips = userTrips.filter(
    t => t.endDate && new Date(t.endDate) < new Date()
  ).length;

  // Unikalne cele podróży - filtrowanie za pomocą obiektu Set eliminującego powtórzenia lokalizacji.
  const uniqueDestinations = new Set(
    userTrips.map(t => t.destination).filter(Boolean)
  ).size;

  const visitedCountries = Array.from(
    new Set(userTrips.map(trip => trip.destination).filter(Boolean))
  ) as string[];

  return (
    <div className="w-full min-h-screen bg-[#F8FAFC] px-6 md:px-10 text-left">
      <div className="w-full max-w-none flex flex-col gap-10">

        <Link
          href="/"
          className="flex items-center gap-2 text-blue-400 font-black text-xs uppercase tracking-widest mb-8 hover:text-blue-600 transition-all w-fit"
        >
          <span className="text-xl">←</span> Powrót do planowania
        </Link>

        <div className="flex flex-col gap-10">

          {/* KARTA PROFILU UŻYTKOWNIKA */}
          <section className="bg-white rounded-[3rem] p-10 shadow-2xl shadow-blue-100/50 border border-blue-50 flex flex-col md:flex-row items-center gap-8">
            <div className="w-32 h-32 bg-blue-600 rounded-[2rem] flex items-center justify-center text-white text-5xl font-black shadow-xl border-4 border-blue-50">
              {session.user?.name?.charAt(0).toUpperCase()}
            </div>
            <div className="text-center md:text-left">
              <h2 className="text-4xl font-black text-blue-900 mb-2">
                {session.user?.name}
              </h2>
              <p className="text-blue-400 font-bold">Aktywny podróżnik</p>
              <div className="flex gap-3 mt-4 justify-center md:justify-start">
                <span className="bg-amber-100 text-amber-600 px-4 py-1.5 rounded-xl text-xs font-black uppercase">
                  🏆 Eksplorer
                </span>
                <span className="bg-blue-100 text-blue-600 px-4 py-1.5 rounded-xl text-xs font-black uppercase">
                  ✈️ {totalTrips} Wypraw
                </span>
              </div>
            </div>
          </section>

          <div className="grid grid-cols-1 xl:grid-cols-12 gap-8">
            {/* STATYSTYKI PODRÓŻNICZE */}
            <div className="xl:col-span-4 grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-1 gap-6">
              <StatCard title="Suma wyjazdów" value={totalTrips} sub="Wszystkie wyprawy" icon="🗺️" />
              <StatCard title="Lokalizacje" value={uniqueDestinations} sub="Odwiedzone miejsca" icon="🌍" />
              <StatCard title="Łączny budżet" value={`${totalExpensesSum.toFixed(2)} zł`} sub="Wydane łącznie" icon="💰" />
              <StatCard title="Zakończone" value={completedTrips} sub="Archiwalne" icon="📅" />
            </div>

            {/* INTEGRACJA MAPY Z INTERFEJSEM UŻYTKOWNIKA */}
            <div className="xl:col-span-8 bg-white rounded-[3rem] p-8 shadow-2xl border border-blue-50 flex flex-col">
              <div className="mb-6">
                <h3 className="text-2xl font-black text-blue-900">Mapa Odkryć</h3>
                <p className="text-[10px] font-black text-blue-300 uppercase tracking-widest">
                  Twoje pinezki na świecie
                </p>
              </div>

              <div className="flex-1 w-full min-h-[420px] lg:min-h-[500px] bg-blue-50/30 rounded-[2.5rem] border-2 border-dashed border-blue-100 flex items-center justify-center overflow-hidden">
                <WorldMap visited={visitedCountries} />
              </div>

              <div className="flex gap-6 mt-8">
                <div className="flex items-center gap-2">
                  <div className="w-3 h-3 bg-blue-600 rounded-full shadow-lg shadow-blue-200" />
                  <span className="text-[10px] font-black text-slate-400 uppercase tracking-widest">
                    Odwiedzone
                  </span>
                </div>
                <div className="flex items-center gap-2">
                  <div className="w-3 h-3 bg-blue-200 rounded-full" />
                  <span className="text-[10px] font-black text-slate-400 uppercase tracking-widest">
                    W planach
                  </span>
                </div>
              </div>
            </div>
          </div>

          {/* SEKCJA ARCHIWALNA PODRÓŻY */}
          <section className="bg-white rounded-[3rem] p-10 shadow-xl border border-blue-50">
            <h3 className="text-2xl font-black text-blue-900 mb-8">
              Archiwum wypraw
            </h3>
            <div className="space-y-4">
              {userTrips.length > 0 ? (
                userTrips.map((trip) => {
                  const isFinished =
                    trip.endDate && new Date(trip.endDate) < new Date();

                  // Obliczenie sumy wydatków dla pojedynczej wycieczki
                  const tripCost =
                    trip.expenses?.reduce(
                      (s, e) => s + (Number(e.amount.toString()) || 0),
                      0
                    ) || 0;

                  return (
                    <ArchiveItem
                      key={trip.id}
                      title={trip.name}
                      date={
                        trip.startDate
                          ? new Date(trip.startDate).toLocaleDateString(
                              'pl-PL',
                              { month: 'long', year: 'numeric' }
                            )
                          : "Brak daty"
                      }
                      cost={`${tripCost.toFixed(2)} zł`}
                      status={isFinished ? "ZAKOŃCZONE" : "PLANOWANE"}
                      isBlue={!isFinished}
                    />
                  );
                })
              ) : (
                <p className="text-slate-400 font-bold py-4">
                  Nie masz jeszcze żadnych wyjazdów.
                </p>
              )}
            </div>
          </section>

        </div>
      </div>
    </div>
  );
}

// Komponent karty statystyk.
function StatCard({ title, value, sub, icon }: StatCardProps) {
  return (
    <div className="bg-white p-8 rounded-[2.5rem] shadow-xl border border-blue-50 text-left">
      <div className="text-3xl mb-4">{icon}</div>
      <p className="text-[10px] font-black text-blue-300 uppercase tracking-widest mb-1">
        {title}
      </p>
      <h3 className="text-3xl font-black text-blue-900">{value}</h3>
      <p className="text-[10px] text-slate-400 mt-1 font-bold">{sub}</p>
    </div>
  );
}

// Komponent elementu listy archiwalnej.
function ArchiveItem({ title, date, cost, status, isBlue }: ArchiveItemProps) {
  return (
    <div
      className={`flex items-center justify-between p-6 rounded-2xl border ${
        isBlue
          ? 'bg-blue-50 border-blue-100'
          : 'bg-slate-50 border-slate-100'
      }`}
    >
      <div className="text-left">
        <h4 className="font-black text-blue-900">{title}</h4>
        <p className="text-xs text-slate-400 font-bold">{date}</p>
      </div>
      <div className="text-right">
        <p className="font-black text-blue-900">{cost}</p>
        <p
          className={`text-[9px] font-black uppercase tracking-widest ${
            isBlue ? 'text-blue-500' : 'text-green-500'
          }`}
        >
          {status}
        </p>
      </div>
    </div>
  );
}
```
